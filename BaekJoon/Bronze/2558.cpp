#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    int n, r;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        vector<bool> a;
        string s;
        cin >> s;

        a.reserve(s.length());
        for (int j = 0; j < s.length(); j++)
        {
            if(a.)
            a.push_back(s[j]);
            
            
        }
    }
}